.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
data/
docker-compose.local.yml

# Страховка: проектные песочницы живут в приватном репозитории лаборатории, а не
# здесь — в них рабочий контекст конкретных проектов (внутренние хосты, ключи
# задач). Каталога с таким именем тут быть не должно; правило на случай, если он
# появится — один `git add -A` опубликовал бы их содержимое.
projects/
